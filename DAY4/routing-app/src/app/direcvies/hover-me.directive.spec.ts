import { HoverMeDirective } from './hover-me.directive';

describe('HoverMeDirective', () => {
  it('should create an instance', () => {
    const directive = new HoverMeDirective();
    expect(directive).toBeTruthy();
  });
});
